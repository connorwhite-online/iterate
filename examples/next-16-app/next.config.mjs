import { withIterate } from "iterate-ui-next";

/** @type {import('next').NextConfig} */
const nextConfig = {};

// See the next-15 example for why appName matters in multi-app repos.
export default withIterate(nextConfig, { appName: "next-16-example" });
