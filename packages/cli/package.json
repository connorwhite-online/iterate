{
  "name": "iterate-ui-cli",
  "version": "0.1.6",
  "description": "CLI for iterate — manage worktree iterations",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/connorwhite-online/iterate",
    "directory": "packages/cli"
  },
  "homepage": "https://iterate-ui.com",
  "keywords": ["iterate", "ui", "cli", "worktree", "ai"],
  "engines": {
    "node": ">=18.0.0"
  },
  "type": "module",
  "bin": {
    "iterate": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist",
    "skills",
    ".claude-plugin"
  ],
  "scripts": {
    "prepublishOnly": "pnpm build",
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "iterate-ui-core": "workspace:*",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.3.0",
    "typescript": "^5.7.0"
  }
}
